<%= heading t('.title') %>

<%= render 'form' %>

<%= link_to t('.destroy'), @quiz, method: :delete, confirm: t('.confirm_destruction', default: 'Are you sure you want to delete this?') %>
<%= link_to t('.cancel'), quizzes_path, class: 'dialog-closer' %>
