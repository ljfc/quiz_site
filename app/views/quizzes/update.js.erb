<%= javascript_flash_messages %>

$('.object.quiz').filter(function() {
  return $(this).data('object-id') == '<%= @quiz.to_param %>'
}).replaceWith('<%= escape_javascript(render(@quiz)) %>');
hideDialog();
