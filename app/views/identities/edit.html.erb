<%= heading t('.title') %>

<%= render 'form' %>

<%= link_to t('.destroy'), @identity, remote: true, method: :delete, confirm: t('.confirm_destruction', default: 'Are you sure you want to delete this?') %>
<%= link_to t('.cancel'), identities_path, class: 'dialog-closer' %>
