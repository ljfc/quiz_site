<%= javascript_flash_messages %>

$('.object.identity').filter(function() {
  return $(this).data('object-id') == '<%= @identity.to_param %>'
}).replaceWith('<%= escape_javascript(render(@identity)) %>');
hideDialog();
