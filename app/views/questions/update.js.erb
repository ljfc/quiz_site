<%= javascript_flash_messages %>

$('.object.question').filter(function() {
  return $(this).data('object-id') == '<%= @question.to_param %>'
}).replaceWith('<%= escape_javascript(render(@question)) %>');
hideDialog();
