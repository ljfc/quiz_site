<%= form_for([@quiz, @question, @possible_answer], remote: true) do |f| %>
  <% if @possible_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@possible_answer.errors.count, "error") %> prohibited this possible_answer from being saved:</h2>

      <ul>
      <% @possible_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :correct %><br />
    <%= f.check_box :correct %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
