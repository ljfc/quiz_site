<%%= heading t('.title') %>

<%%= render 'form' %>

<%%= link_to t('.destroy'), @<%= singular_table_name %>, method: :delete, data: { confirm: t('.confirm_destruction') } %>
<%%= link_to t('.cancel'), <%= index_helper %>_path, class: 'dialog-closer' %>
