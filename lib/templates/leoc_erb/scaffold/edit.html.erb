<%%= heading t('.title') %>

<%%= render 'form' %>

<%%= link_to t('.destroy'), @<%= singular_table_name %>, remote: true, method: :delete, confirm: t('.confirm_destruction', default: 'Are you sure you want to delete this?') %>
<%%= link_to t('.cancel'), <%= index_helper %>_path, class: 'dialog-closer' %>
